# Sample environment file. Copy to .env and fill in your own values.
#   cp .env.sample .env
# Real .env files are gitignored and must never be committed.

OPENAI_API_KEY=gsk_your_groq_api_key_here
# deepeval's judge reads this when USE_LOCAL_MODEL=YES (same Groq key)
LOCAL_MODEL_API_KEY=gsk_your_groq_api_key_here
