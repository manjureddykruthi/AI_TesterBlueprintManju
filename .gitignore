# Maven / Java
target/
*.class
*.jar
*.war
*.log

# IDE
.idea/
.vscode/
*.iml
.project
.classpath
.settings/

# OS
.DS_Store
Thumbs.db

# Local Claude config
.claude/

# IDE / editor scratch
.commandcode/
.env
env

# Local browser verification artifacts
.playwright-mcp/

# Python virtualenvs (uv sync creates these per-chapter).
# Both spellings: chapters 15 and 16 use a bare `venv/`.
.venv/
**/.venv/
venv/
**/venv/

# Node
node_modules/
**/node_modules/
dist/
**/dist/

# Local vector stores (ChromaDB / Qdrant on-disk collections)
**/chroma_db/
**/qdrant_storage/

# Backups of secret files - `.env` does not match `.env.bak.123`
.env.bak*
**/.env.bak*
.env.local

# Python bytecode
__pycache__/
*.py[cod]

# LangFlow local runtime state (Docker volume + Chroma dumps, ~150MB)
chapter_05_AI_Agents_LangFlow/langflow-data/
chapter_05_AI_Agents_LangFlow/chroma_backups/
.env
# DeepEval local provider config, run cache and telemetry
**/.deepeval/
.env.local

# pytest cache
.pytest_cache/

# RAG Explorer: files uploaded through the UI at runtime, not source.
# Keeps personal documents (resumes, client test plans) out of the repo.
chapter_16_DeepEval_Framwork/02_RAG_Explorer/02_rag_explorer/data/uploads/

# Vercel
.vercel/
**/.vercel/
